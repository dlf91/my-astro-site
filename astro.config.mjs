// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://dlf91.github.io',
  base: '/', // Only if not using your-username.github.io
});
